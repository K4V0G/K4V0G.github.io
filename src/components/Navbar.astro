<nav
  id="navbar"
  class="bg-black shadow-2xl border-b border-white/10 block top-0 left-0 right-0 z-50 slideDownFadeIn"
>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="relative flex h-18 sm:h-23 items-center justify-between">
      <!-- Botón menú móvil -->
      <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
        <button type="button" class="relative inline-flex items-center justify-center rounded-md p-2 text-white hover:bg-white/10 focus:ring-2 focus:ring-white focus:outline-none focus:ring-inset" aria-controls="mobile-menu" aria-expanded="false">
          <span class="sr-only">Abrir Menú</span>
          <!-- Icono menú cerrado -->
          <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <!-- Icono menú abierto -->
          <svg class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>

        <!-- Logo -->
        <div class="flex flex-1 items-center justify-center sm:justify-start">
        <div class="flex shrink-0 items-center">
            <a href="/" class="group inline-block">
            <img 
                class="h-12 sm:h-14 w-auto transform transition-transform duration-300 group-hover:scale-120" 
                src="/K4V0G_logo2.webp" 
                alt="K4V0G Logo"
            >
            </a>
        </div>
        </div>

        <!-- Menú principal con líneas verticales -->
        <div class="hidden sm:flex sm:ml-6 items-center space-x-6 px-4 border-l-2 border-r-2 border-white">
        <a href="/" class="relative group text-lg font-semibold text-white transition-colors duration-400 px-4 py-2 rounded-md">
            Inicio
            <span class="absolute left-0 bottom-0 w-0 h-0.5 bg-slate-100 transition-all group-hover:w-full"></span>
        </a>
        <a href="#" class="relative group text-lg font-semibold text-white transition-colors duration-400 px-4 py-2 rounded-md">
            Team
            <span class="absolute left-0 bottom-0 w-0 h-0.5 bg-slate-100 transition-all group-hover:w-full"></span>
        </a>
        <a href="#" class="relative group text-lg font-semibold text-white transition-colors duration-400 px-4 py-2 rounded-md">
            Proyectos
            <span class="absolute left-0 bottom-0 w-0 h-0.5 bg-slate-100 transition-all group-hover:w-full"></span>
        </a>
        <a href="#" class="relative group text-lg font-semibold text-white transition-colors duration-400 px-4 py-2 rounded-md">
            Writeups
            <span class="absolute left-0 bottom-0 w-0 h-0.5 bg-slate-100 transition-all group-hover:w-full"></span>
        </a>
            <a href="#" class="relative group text-lg font-semibold text-white transition-colors duration-400 px-4 py-2 rounded-md">
            Hacademy
            <span class="absolute left-0 bottom-0 w-0 h-0.5 bg-slate-100 transition-all group-hover:w-full"></span>
        </a>
        </div>

      <!-- Placeholder para la derecha si quisieras poner botones o avatar -->
      <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
        <!-- Aquí puedes poner íconos o perfil de usuario -->
      </div>
    </div>
  </div>

  <!-- Menú móvil -->
  <div class="sm:hidden" id="mobile-menu">
    <div class="space-y-1 px-2 pt-2 pb-3 bg-black border-t border-white/10">
      <a href="#" class="block rounded-md px-3 py-2 text-base font-medium text-white hover:bg-white/10 hover:text-white">Inicio</a>
      <a href="#" class="block rounded-md px-3 py-2 text-base font-medium text-white hover:bg-white/10 hover:text-white">Team</a>
      <a href="#" class="block rounded-md px-3 py-2 text-base font-medium text-white hover:bg-white/10 hover:text-white">Proyectos</a>
      <a href="#" class="block rounded-md px-3 py-2 text-base font-medium text-white hover:bg-white/10 hover:text-white">Writeups</a>
      <a href="#" class="block rounded-md px-3 py-2 text-base font-medium text-white hover:bg-white/10 hover:text-white">Academia</a>
    </div>
  </div>
</nav>


<style>
  @keyframes slideDownFadeIn {
  0% {
    transform: translateY(-100%);
    opacity: 0;
  }
  100% {
    transform: translateY(0);
    opacity: 1;
  }
}

.slideDownFadeIn {
  animation: slideDownFadeIn 0.7s ease forwards;
}
</style>